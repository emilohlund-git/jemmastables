mutation Register($options: AdminNamePasswordInput!) {
  register(options: $options) {
    errors {
      field
      message
    }
    admin {
      id
      createdAt
      updatedAt
      username
    }
  }
}
