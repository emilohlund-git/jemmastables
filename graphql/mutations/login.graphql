mutation Login($options: AdminNamePasswordInput!) {
  login(options: $options) {
    errors {
      field
      message
    }
    admin {
      id
      createdAt
      updatedAt
      username
    }
  }
}
