query AvailableTimeByMilliseconds($date: String!) {
  availableTimeByMilliseconds(date: $date) {
    id
    availableTimeId
    createdAt
    updatedAt
    timeTo
    timeFrom
    type
    date
  }
}
